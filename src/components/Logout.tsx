'use client'
import React from 'react'

export const MyComponent = ({ label }: { label: string }) => {
  return <button>{label}</button>
}

export const MyComponent1 = () => {
  return <button>Đăng xuất Custom1111</button>
}
